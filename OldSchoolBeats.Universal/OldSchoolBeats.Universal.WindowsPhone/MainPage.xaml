<Page
    x:Class="OldSchoolBeats.Universal.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OldSchoolBeats.Universal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" DataContext="{Binding Main.DataService, Source={StaticResource Locator}}">
    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.Background>
                <SolidColorBrush Color="#FF1F1F1F" Opacity="0.1"/>
            </CommandBar.Background>
            <AppBarButton Icon="Add" Label="add"/>
            <AppBarButton Icon="Edit" Label="edit"/>
            <AppBarButton Icon="Delete" Label="delete"/>
            <AppBarButton Icon="Cancel" Label="cancel"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid DataContext="{Binding Source={StaticResource Locator}}">
        <StackPanel>
            <TextBlock FontSize="22" FontFamily="Fonts/Grand Stylus.ttf#Grand Stylus" TextAlignment="Center">OldSchool Artists</TextBlock>
            <ListView x:Name="lstArtists" ItemTemplate="{StaticResource ArtistListDataTemplate}" DataContext="{Binding Main.DataService}" ItemsSource="{Binding Items}"/>
        </StackPanel>

    </Grid>
</Page>